<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !curret_user?(user) %>
    <%= link_to "delte", user, method: :delete, data: { confirm: "You sure" }%>
<% end %>
</li>
